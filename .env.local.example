# M&A Integration Engine — Environment Variables
# Copy this file to .env.local and fill in your values

# Claude API (Anthropic) — Required for AI guidance
ANTHROPIC_API_KEY=sk-ant-...

# Neon Postgres — deals are persisted here and synced across sessions/devices
# Get your connection string from console.neon.tech → your project → Connection Details
# If omitted, localStorage-only mode is used (single-browser, no sharing)
DATABASE_URL=postgresql://user:password@ep-xxxx.us-east-1.aws.neon.tech/main?sslmode=require
